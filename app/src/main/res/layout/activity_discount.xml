<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:orientation="vertical"
    android:background="#f5f3f4"
    android:layout_height="match_parent"
    tools:context=".DiscountActivity">

  <TextView
      android:layout_marginTop="40dp"
      android:layout_centerHorizontal="true"
      android:id="@+id/heading"
      android:textSize="30sp"
      android:fontFamily="@font/latom"
      android:textColor="#000"
      android:text="Send Money"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"/>


    <EditText
        android:id="@+id/client_search_input"
        android:layout_width="298dp"
        android:layout_height="41dp"
        android:layout_below="@+id/heading"
        android:layout_alignParentStart="true"
        android:layout_marginStart="14dp"
        android:layout_marginTop="59dp"
        android:layout_marginEnd="11dp"
        android:layout_marginBottom="8dp"
        android:layout_toStartOf="@+id/btn_client_search"
        android:background="@drawable/edit_text_style"
        android:drawableLeft="@drawable/ic_baseline_search_24"
        android:drawablePadding="10dp"
        android:paddingLeft="10dp" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/client_container"
        android:layout_below="@+id/client_search_input"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

    </androidx.recyclerview.widget.RecyclerView>
     <ProgressBar
         android:id="@+id/_progress"
         android:layout_alignParentBottom="true"
         android:layout_centerHorizontal="true"
         android:layout_width="wrap_content"
         android:layout_height="wrap_content"/>

    <Button
        android:id="@+id/btn_client_search"
        android:layout_width="78dp"
        android:layout_height="40dp"
        android:layout_above="@+id/client_container"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="10dp"
        android:layout_marginRight="7dp"
        android:layout_marginBottom="11dp"
        android:background="@drawable/butttonstyle"
        android:fontFamily="@font/artifika"
        android:text="Search"
        android:textAllCaps="false"
        android:textColor="#ffffff" />

</RelativeLayout>