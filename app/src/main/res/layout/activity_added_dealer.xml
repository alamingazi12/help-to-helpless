<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_margin="5dp"
    android:background="#f5f3f4"
    android:layout_height="match_parent"
    tools:context=".AddedDealerActivity">

    <TextView

        android:id="@+id/heading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="50dp"
        android:layout_marginEnd="75dp"
        android:fontFamily="@font/alexbold"
        android:text="Send Money"
        android:textColor="#000"
        android:textSize="32sp" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/added_dealer_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/edittext_search_dealer"
        android:layout_marginTop="20dp">

    </androidx.recyclerview.widget.RecyclerView>
   <ProgressBar
       android:layout_centerHorizontal="true"
       android:layout_alignParentBottom="true"
       android:id="@+id/_progress"
       android:layout_width="50dp"
       android:layout_height="50dp"/>

    <EditText
        android:id="@+id/edittext_search_dealer"
        android:layout_width="match_parent"
        android:layout_height="40sp"
        android:drawablePadding="8dp"
        android:paddingLeft="10dp"
        android:layout_below="@+id/heading"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="40dp"
        android:background="@drawable/edit_text_style"
        android:drawableLeft="@drawable/ic_baseline_search_24"
        android:ems="10"
        android:fontFamily="@font/latom"
        android:layout_marginLeft="20sp"
        android:layout_marginRight="20sp"
        android:inputType="textPersonName"
        android:hint="Search" />
</RelativeLayout>